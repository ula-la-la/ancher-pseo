interface:
  display_name: "Publish X artifacts"
  short_description: "Turn approved X content into reviewed Ancher artifacts"
  default_prompt: "Use $publish-x-artifacts to run the approved X source-to-Ancher Artifact workflow through the private review gate."
